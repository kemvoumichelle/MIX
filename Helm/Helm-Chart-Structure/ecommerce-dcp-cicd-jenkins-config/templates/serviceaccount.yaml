{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ecommerce-dcp-cicd-jenkins-config.serviceAccountName" . }}
  labels:
    {{- include "ecommerce-dcp-cicd-jenkins-config.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
