apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecommerce-dcp-cicd-jenkins-config.fullname" . }}-test-connection"
  labels:
    {{- include "ecommerce-dcp-cicd-jenkins-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecommerce-dcp-cicd-jenkins-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
